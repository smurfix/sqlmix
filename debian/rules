#! /usr/bin/make -f

export PYBUILD_NAME=sqlmix
%:
	dh $@ --with python2,python3 --buildsystem=pybuild

override_dh_auto_build:
	dh_auto_build
	yapps sql_diff.g
	mv sql_diff.py sql_diff
	chmod +x sql_diff


	
